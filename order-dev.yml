spring:
  application:
    name: order
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    username: root
    password: 123456
    url: jdbc:mysql://47.103.34.27:3306/SpringCloud_Sell?characterEncoding=utf-8&serverTimezone=UTC&useSSL=false
  jpa:
    show-sql: true
#    database-platform: org.hibernate.dialect.MySQL5Dialect
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true
    instance-id=${spring:cloud:client:ipAddress}:${server:port}:
env:
  dev
